from sklearn.preprocessing import StandardScaler

from data_loader import create_som_data, som_pak_data_load
from som import SOM
from som_evaluator import SOMEvaluator
from som_visualizer import SOMVisualizer

filepath = "animal.dat"

data = create_som_data(som_pak_data_load(filepath))
print(data.data)
print(data.target)
print(data.target_names)

